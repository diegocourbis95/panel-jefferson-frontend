import { useEffect, useState } from 'react'
import { Link, useParams } from 'react-router-dom'
import { get, patch } from '../api/client'
import { Badge, Empty } from '../components/UI'
import { clp, date } from '../utils'
export default function ClienteDetalle(){const {id}=useParams();const [data,setData]=useState(null),[contact,setContact]=useState('');const load=()=>Promise.all([get(`/clientes/${id}`),get('/documentos',{params:{cliente_id:id}}),get('/tareas')]).then(([cliente,docs,tasks])=>{setData({cliente,docs,tasks:tasks.filter(t=>t.cliente_id===id)});setContact(cliente.proximo_contacto||'')});useEffect(() => { void load() },[id]);if(!data)return <p className="loading">Cargando cliente…</p>;async function save(){await patch(`/clientes/${id}`,{proximo_contacto:contact||null});load()}return <><Link className="back" to="/clientes">← Clientes</Link><header><Badge variant={data.cliente.tipo}>{data.cliente.tipo}</Badge><h1>{data.cliente.nombre}</h1><p className="muted">{data.cliente.rut||'Sin RUT'} · {data.cliente.telefono||'Sin teléfono'} · {data.cliente.email||'Sin email'}</p></header><section className="two-col"><article className="panel"><h2>Próximo contacto</h2><div className="inline"><input type="date" value={contact} onChange={e=>setContact(e.target.value)}/><button className="primary" onClick={save}>Guardar</button></div><p>{data.cliente.notas||'Sin notas.'}</p></article><article className="panel"><h2>Tareas asociadas</h2>{data.tasks.length?data.tasks.map(t=><div className="compact" key={t.id}><b>{t.texto}</b><Badge variant={t.categoria}>{t.columna}</Badge></div>):<Empty/>}</article></section><section className="panel"><h2>Documentos asociados</h2>{data.docs.length?data.docs.map(d=><div className="compact" key={d.id}><div><b>{d.tipo} N°{d.numero_documento}</b><span>Vence {date(d.fecha_vencimiento)}</span></div><b>{clp(d.saldo_pendiente)}</b></div>):<Empty/>}</section></>}
